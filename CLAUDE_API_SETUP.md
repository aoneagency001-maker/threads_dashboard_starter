# 🔑 Как получить Claude API ключ

## Шаг 1: Регистрация на Anthropic Console

1. Перейдите на сайт: **https://console.anthropic.com/**
2. Нажмите **"Sign Up"** (Зарегистрироваться)
3. Введите email и пароль, либо войдите через Google

## Шаг 2: Пополнение баланса

⚠️ **ВАЖНО:** Claude API работает на предоплате (prepaid credits)

1. Перейдите в раздел **"Settings"** → **"Plans & Billing"**
2. Нажмите **"Purchase credits"** (Купить кредиты)
3. Выберите сумму:
   - **$5** - минимум (хватит на 1-2 книги для тестов)
   - **$20** - рекомендовано (хватит на 5-10 книг)
   - **$50+** - если планируете обрабатывать много книг

4. Оплатите картой (принимаются международные карты)

💡 **Стоимость обработки одной книги:**
- С Claude 3.5 Sonnet: **$5-12** за книгу (~200-300 страниц)
- Зависит от размера и сложности текста

## Шаг 3: Создание API ключа

1. В консоли перейдите в раздел **"API Keys"** (слева в меню)
2. Нажмите **"Create Key"** (Создать ключ)
3. Дайте ключу имя, например: `threads_dashboard_key`
4. Нажмите **"Create Key"**
5. **СКОПИРУЙТЕ ключ** - он показывается только один раз!
   - Формат: `sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

⚠️ **ВАЖНО:** Сохраните ключ в безопасное место! После закрытия окна вы его больше не увидите.

## Шаг 4: Добавление ключа в проект

1. Откройте файл `.env` в корне проекта (или создайте, если его нет)
2. Добавьте строку:

```bash
# Anthropic Claude API
ANTHROPIC_API_KEY=sk-ant-api03-ваш-ключ-здесь

# OpenAI API (можно оставить для резервного использования)
OPENAI_API_KEY=ваш-openai-ключ-если-есть

# Threads API (для публикации)
THREADS_ACCESS_TOKEN=ваш-threads-токен
IG_USER_ID=ваш-instagram-user-id
```

3. Сохраните файл

## Шаг 5: Установка библиотеки Anthropic

Выполните в терминале:

```bash
pip install anthropic
```

## Шаг 6: Проверка работы

Запустите тестовый скрипт:

```bash
python -c "from anthropic import Anthropic; client = Anthropic(); print('✅ Claude API работает!')"
```

Если увидите "✅ Claude API работает!" - всё готово!

## 🎯 Модели Claude (рекомендации)

### Claude 3.5 Sonnet (рекомендуется)
- **Модель:** `claude-3-5-sonnet-20241022`
- **Качество:** ⭐⭐⭐⭐⭐ Отлично для анализа текста
- **Стоимость:** $3 за 1M input tokens, $15 за 1M output tokens
- **Использование:** Основной анализ и извлечение цитат

### Claude 3 Haiku (бюджетный вариант)
- **Модель:** `claude-3-haiku-20240307`
- **Качество:** ⭐⭐⭐ Хорошо для простых задач
- **Стоимость:** $0.25 за 1M input tokens, $1.25 за 1M output tokens
- **Использование:** Перевод, простая фильтрация

## 📊 Расчёт стоимости

### Пример: Книга на 200 страниц

**Input (чтение текста):**
- ~100,000 tokens (вход)
- $3 × 0.1 = **$0.30**

**Output (генерация цитат):**
- ~20,000 tokens (выход)
- $15 × 0.02 = **$0.30**

**Итого:** ~**$0.60-1.50** за книгу (в зависимости от количества проходов)

С учётом анализа качества, перевода и улучшения: **$5-12** за книгу.

## 🔒 Безопасность

1. **Никогда** не коммитьте `.env` файл в Git (уже добавлен в .gitignore)
2. **Не делитесь** API ключом ни с кем
3. Если ключ скомпрометирован - удалите его в консоли и создайте новый
4. Установите **"Usage limits"** в настройках (например, $50/месяц)

## 📞 Поддержка

- **Документация Claude API:** https://docs.anthropic.com/
- **Discord сообщество:** https://discord.gg/anthropic
- **Email поддержки:** support@anthropic.com

## ✅ Готово!

После выполнения всех шагов система будет использовать Claude для:
- 🧠 Интеллектуального анализа текста
- 📝 Извлечения осмысленных цитат
- 🔍 Проверки качества и полноты
- 🌐 Перевода на русский язык
- ⚡ Категоризации и метаданных

**Качество цитат вырастет в 5-10 раз!**
