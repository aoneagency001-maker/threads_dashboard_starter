# 🎯 Интеграция Anthropic Claude API

## ✅ Что изменилось

Мы заменили **GPT-4o-mini на Claude (Anthropic)** для всех функций **написания и редактирования текстов**.

## 🔄 Приоритет использования моделей

Теперь система работает по следующему приоритету:

1. **Claude (Anthropic)** - основной выбор для написания/редактирования текстов
2. **GPT-4o-mini (OpenAI)** - fallback, если Claude недоступен
3. **Локальная валидация** - если ни одна LLM недоступна

## 📋 Где используется Claude

### ✅ Функции, где GPT заменен на Claude:

1. **`_refine_batch()`** в `backend/agent.py`
   - Полировка и редактирование цитат батчами
   - Проверка завершенности, длины, осмысленности
   - Добавление метаданных

2. **`_extract_engaging_from_chunk()`** в `backend/agent.py`
   - Извлечение 1-2 ключевых идей из текстового фрагмента
   - Создание готовых цитат для Threads

3. **`translate_text()`** в `backend/parser.py`
   - Перевод цитат на русский (или другой язык)
   - Сохранение стиля и тона

4. **`_analyze_quote_quality()`** в `backend/smart_quote_extractor.py`
   - Анализ качества цитаты по 5 критериям
   - Оценка завершенности, осмысленности, практической ценности

### ❌ Функции, которые НЕ изменились (используют Gemini или остаются без LLM):

1. **Извлечение цитат из книг** - использует **Gemini** (дешевле и быстрее)
2. **Валидация цитат** - использует локальные правила (без LLM)
3. **Определение темы книги** - пока остается GPT (можно оставить)

## 🚀 Настройка Claude API

### 1. Получите API ключ

1. Перейдите на https://console.anthropic.com/
2. Войдите или создайте аккаунт
3. Перейдите в раздел "API Keys"
4. Создайте новый API ключ
5. Скопируйте ключ (начинается с `sk-ant-...`)

### 2. Добавьте в .env файл

```env
ANTHROPIC_API_KEY=sk-ant-your-key-here
```

### 3. Установите зависимости

```bash
pip install anthropic>=0.34.0
```

Или обновите все зависимости:

```bash
pip install -r requirements.txt
```

## 💰 Стоимость Claude

- **Claude 3.5 Sonnet** (используется по умолчанию):
  - Входные токены: $3/1M
  - Выходные токены: $15/1M

- **Claude 3 Haiku** (более дешевая альтернатива):
  - Входные токены: $0.25/1M
  - Выходные токены: $1.25/1M

**Примечание:** Можно изменить модель в `backend/claude_client.py` → `claude_complete()` → параметр `model`.

## 🎯 Преимущества Claude

1. **Лучшее качество текста** - Claude известен лучшим пониманием контекста
2. **Лучшая структурированность** - отличная работа с JSON
3. **Более естественный язык** - лучше передает смысл

## 🔄 Как работает fallback

Если Claude недоступен (нет API ключа или ошибка):

1. Система автоматически переключается на GPT-4o-mini
2. Если GPT тоже недоступен - использует локальную валидацию
3. Никаких ошибок - система всегда работает

## 📊 Сравнение: Gemini vs Claude vs GPT

| Задача | Используемая модель | Почему |
|--------|---------------------|--------|
| **Извлечение цитат** | Gemini 2.5 Flash | Дешево ($0.075/1M) и быстро (вся книга за раз) |
| **Полировка цитат** | Claude 3.5 Sonnet | Лучшее качество редактирования |
| **Перевод** | Claude 3.5 Sonnet | Более естественный перевод |
| **Анализ качества** | Claude 3.5 Sonnet | Лучшее понимание контекста |

## 🧪 Тестирование

После добавления API ключа Claude, система автоматически начнет его использовать. Вы можете проверить:

1. Запустите `streamlit run app.py`
2. Выберите книгу
3. Нажмите "🧠 Улучшить цитаты"
4. В логах вы увидите: `✅ Используется Claude для полировки цитат`

Если Claude недоступен, в логах будет: `⚠️ Ошибка Claude, используем fallback: ...`

## 🐛 Решение проблем

### "ModuleNotFoundError: No module named 'anthropic'"

```bash
pip install anthropic>=0.34.0
```

### "Anthropic API key not found"

Проверьте `.env` файл - должен быть `ANTHROPIC_API_KEY=...`

### "Claude не используется, используется GPT"

1. Проверьте что ключ правильно добавлен в `.env`
2. Перезапустите приложение
3. Проверьте логи - там будет сообщение об ошибке если есть

---

**Обновлено:** 2024-11-03  
**Версия:** 1.0

