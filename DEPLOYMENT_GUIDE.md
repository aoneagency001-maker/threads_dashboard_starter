# 🚀 Как получить публичную ссылку на приложение

## Вариант 1: Streamlit Cloud (РЕКОМЕНДУЕТСЯ) 🌟

**Бесплатный хостинг от Streamlit - даёт публичную ссылку!**

### Шаги:

1. **Зайдите на Streamlit Cloud**
   ```
   https://share.streamlit.io/
   ```

2. **Войдите через GitHub**
   - Нажмите "Sign in with GitHub"
   - Разрешите доступ к репозиториям

3. **Создайте новое приложение**
   - Нажмите "New app"
   - Выберите репозиторий: `aoneagency001-maker/threads_dashboard_starter`
   - Выберите ветку: `claude/explore-repository-011CUfyPtqRrqx1bvC1nDkCU`
   - Main file path: `app.py`
   - Нажмите "Deploy!"

4. **Получите публичную ссылку**
   - Через 2-3 минуты ваше приложение будет доступно
   - Ссылка будет вида: `https://your-app-name.streamlit.app`
   - Эту ссылку можно шарить кому угодно!

### ⚙️ Настройка Secrets (для API ключей):

В Streamlit Cloud:
1. Откройте Settings приложения
2. Перейдите в "Secrets"
3. Добавьте ваши ключи:

```toml
ANTHROPIC_API_KEY = "sk-ant-api03-ВАШ_КЛЮЧ_ЗДЕСЬ"

# Опционально для Threads:
# THREADS_ACCESS_TOKEN = "your_token"
# IG_USER_ID = "your_id"
```

4. Сохраните и перезапустите приложение

---

## Вариант 2: Локальный доступ 💻

**Для работы на вашем компьютере:**

### Если вы на своём компьютере:
```
http://localhost:8501
```

### Если на удалённом сервере:
```
http://ВАШ_IP_АДРЕС:8501
```

**Узнать IP адрес:**
```bash
curl ifconfig.me
```

---

## Вариант 3: Ngrok (временный туннель) 🌐

**Даёт публичную ссылку для локального приложения:**

1. **Установите ngrok:**
   ```bash
   # macOS
   brew install ngrok

   # Linux
   wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
   tar xvzf ngrok-*.tgz
   sudo mv ngrok /usr/local/bin
   ```

2. **Зарегистрируйтесь:**
   ```
   https://dashboard.ngrok.com/signup
   ```

3. **Настройте токен:**
   ```bash
   ngrok config add-authtoken ВАШ_ТОКЕН
   ```

4. **Запустите туннель:**
   ```bash
   ngrok http 8501
   ```

5. **Получите ссылку:**
   - Ngrok выдаст ссылку вида: `https://abc123.ngrok.io`
   - Эта ссылка работает пока ngrok запущен

---

## Создать Pull Request (для просмотра кода) 📝

**Ссылка для создания PR:**

```
https://github.com/aoneagency001-maker/threads_dashboard_starter/compare/main...claude/explore-repository-011CUfyPtqRrqx1bvC1nDkCU?expand=1
```

**Или вручную:**
1. Зайдите на https://github.com/aoneagency001-maker/threads_dashboard_starter
2. Нажмите "Pull requests" → "New pull request"
3. base: `main` ← compare: `claude/explore-repository-011CUfyPtqRrqx1bvC1nDkCU`
4. Нажмите "Create pull request"
5. Добавьте описание и создайте PR

---

## 📊 Что вы увидите в приложении

После деплоя вы сможете:

✅ Загружать PDF книги
✅ Извлекать цитаты через Claude AI
✅ Просматривать цитаты с фильтрацией
✅ Искать по ключевым словам
✅ Публиковать в Threads
✅ Просматривать статистику

---

## 🎯 Мои рекомендации:

### Для постоянного использования:
**→ Streamlit Cloud** (бесплатно, публичная ссылка, автообновление при git push)

### Для быстрого теста:
**→ Локально** (localhost:8501)

### Для демо клиентам:
**→ Ngrok** (быстрая временная ссылка)

---

## 🆘 Проблемы?

### Streamlit Cloud не деплоит:
- Проверьте requirements.txt
- Добавьте secrets (API ключи)
- Посмотрите логи в интерфейсе

### Localhost не открывается:
```bash
# Проверить запущен ли Streamlit
ps aux | grep streamlit

# Перезапустить
pkill -f streamlit
streamlit run app.py
```

---

## ✅ Выберите вариант и я помогу!

Какой вариант хотите использовать?
1. Streamlit Cloud (публичная ссылка)
2. Локально (localhost)
3. Ngrok (временная публичная ссылка)
