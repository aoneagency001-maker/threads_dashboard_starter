# 📚 Полная документация проекта Threads Dashboard Starter

**Дата обновления:** 2024-11-03  
**Версия:** 2.0

---

## 📋 Оглавление

1. [Обзор проекта](#обзор-проекта)
2. [Что было сделано в этой сессии](#что-было-сделано-в-этой-сессии)
3. [Архитектура проекта](#архитектура-проекта)
4. [Используемые LLM модели](#используемые-llm-модели)
5. [Интеграция Claude API](#интеграция-claude-api)
6. [Структура файлов](#структура-файлов)
7. [Основные функции](#основные-функции)
8. [API и интеграции](#api-и-интеграции)
9. [Настройка и установка](#настройка-и-установка)

---

## 🎯 Обзор проекта

**Threads Dashboard Starter** - полнофункциональная система для:

- 📖 **Извлечения инсайтов из книг (PDF)** с помощью AI
- 🧠 **Анализа и улучшения цитат** для публикации
- 🚀 **Автоматической публикации в Threads** (Instagram)
- 📊 **Управления базой цитат** с современным веб-интерфейсом

### Основные возможности:

✅ Глубокое сканирование книг с Gemini AI  
✅ Умное извлечение цитат с анализом качества  
✅ Многоэтапная валидация для Threads  
✅ Полировка и улучшение текстов с Claude  
✅ Публикация через официальный Threads API  
✅ Современный темный интерфейс на Streamlit  
✅ Админ-панель для управления токенами  

---

## 🔄 Что было сделано в этой сессии

### 1. **Изучение и синхронизация проекта**

- ✅ Изучена полная структура проекта
- ✅ Проанализированы все файлы и документация
- ✅ Обновлен главный README.md с полным описанием
- ✅ Проверена синхронизация с GitHub

### 2. **Анализ использования LLM моделей**

- ✅ Создан документ `LLM_USAGE_ANALYSIS.md`
- ✅ Проанализированы все этапы обработки
- ✅ Определены функции, которые используют GPT
- ✅ Определены функции, которые используют Gemini

### 3. **Интеграция Anthropic Claude API**

- ✅ Создан модуль `backend/claude_client.py`
- ✅ Заменен GPT на Claude в функциях написания/редактирования:
  - Полировка цитат (`_refine_batch`)
  - Извлечение из фрагментов (`_extract_engaging_from_chunk`)
  - Перевод текстов (`translate_text`)
  - Анализ качества (`_analyze_quote_quality`)
- ✅ Реализован fallback механизм (Claude → GPT → локальная валидация)
- ✅ Обновлен `requirements.txt`
- ✅ Обновлен `env_template.txt`
- ✅ Создана документация `CLAUDE_INTEGRATION.md`

### 4. **Оптимизация архитектуры**

- ✅ Определено, что Gemini используется для извлечения (дешевле и быстрее)
- ✅ Определено, что Claude используется для написания/редактирования (лучшее качество)
- ✅ GPT остается как fallback

---

## 🏗️ Архитектура проекта

```
threads_dashboard_starter/
├── app.py                      # Основное приложение Streamlit
├── app_gemini.py              # Альтернативное приложение с Gemini
│
├── backend/                   # Основная логика
│   ├── agent.py               # Агент для обработки цитат (Claude/GPT)
│   ├── parser.py              # Извлечение текста из PDF + перевод (Claude/GPT)
│   ├── gemini_extractor.py   # Глубокое сканирование с Gemini
│   ├── gemini_book_analyzer.py # Структурированный анализ с Gemini
│   ├── smart_quote_extractor.py # Умный экстрактор (Claude/GPT)
│   ├── quote_validator.py     # Многоэтапная валидация (локальная)
│   └── claude_client.py       # Клиент Claude API (НОВЫЙ)
│
├── pages/                     # Дополнительные страницы Streamlit
│   └── 1_⚙️_Настройки.py      # Админ-панель
│
├── threads_api/               # Библиотека threads-api
│   └── src/
│       └── threads_api.py    # Неофициальный API Threads
│
├── data/
│   ├── books/                 # PDF книги для анализа
│   └── quotes/                # Извлеченные цитаты (JSON)
│
├── examples/                  # Примеры использования API
│   ├── private_api_examples.py
│   └── public_api_examples.py
│
└── requirements.txt           # Зависимости Python
```

---

## 🤖 Используемые LLM модели

### **1. Google Gemini 2.5 Flash**

**Используется для:**
- ✅ Глубокое сканирование книг (извлечение ВСЕХ инсайтов за один запрос)
- ✅ Структурированное извлечение по главам и категориям

**Преимущества:**
- 💰 Очень дешево: $0.075 за 1M токенов (в 200 раз дешевле GPT!)
- ⚡ Быстро: вся книга обрабатывается за 1-2 запроса
- 📚 Огромный контекст: до 2M токенов (вся книга сразу)

**Где используется:**
- `backend/gemini_extractor.py` → `extract_all_insights_from_text()`
- `backend/gemini_book_analyzer.py` → `extract_insights_from_chapter()`

---

### **2. Anthropic Claude 3.5 Sonnet**

**Используется для:**
- ✅ Полировка и редактирование цитат
- ✅ Извлечение идей из фрагментов текста
- ✅ Перевод текстов
- ✅ Анализ качества цитат

**Преимущества:**
- ✍️ Лучшее качество написания/редактирования текстов
- 🎯 Отличная работа с JSON
- 💡 Лучшее понимание контекста

**Где используется:**
- `backend/agent.py` → `_refine_batch()` (через `claude_client.py`)
- `backend/agent.py` → `_extract_engaging_from_chunk()` (через `claude_client.py`)
- `backend/parser.py` → `translate_text()` (через `claude_client.py`)
- `backend/smart_quote_extractor.py` → `_analyze_quote_quality()` (через `claude_client.py`)

**Приоритет:** Основной выбор → если недоступен, используется GPT как fallback

---

### **3. OpenAI GPT-4o-mini**

**Используется для:**
- ⚠️ Fallback, если Claude недоступен
- ⚠️ Определение темы книги (пока не заменено)

**Статус:** Резервный вариант, постепенно заменяется на Claude

---

## 📊 Сравнительная таблица моделей

| Модель | Задача | Стоимость | Скорость | Контекст |
|--------|--------|-----------|----------|----------|
| **Gemini 2.5 Flash** | Извлечение цитат | $0.075/1M | ⚡⚡⚡ | 2M токенов |
| **Claude 3.5 Sonnet** | Написание/редактирование | $3-15/1M | ⚡⚡ | 200K токенов |
| **GPT-4o-mini** | Fallback | $15/1M | ⚡⚡ | 128K токенов |

---

## 🔧 Интеграция Claude API

### Реализованные функции в `claude_client.py`:

1. **`claude_refine_quotes()`** - полировка цитат батчами
2. **`claude_extract_from_chunk()`** - извлечение идей из фрагментов
3. **`claude_translate()`** - перевод текстов
4. **`claude_analyze_quality()`** - анализ качества цитат

### Приоритет использования:

```
1. Claude (если ANTHROPIC_API_KEY настроен)
   ↓ (если недоступен)
2. GPT-4o-mini (если OPENAI_API_KEY настроен)
   ↓ (если недоступен)
3. Локальная валидация (всегда работает)
```

---

## 📁 Структура файлов

### Основные приложения:

- **`app.py`** - главное приложение с поддержкой:
  - Глубокое сканирование (Gemini)
  - Стандартное извлечение (GPT/Claude)
  - Улучшение цитат
  - Публикация в Threads

- **`app_gemini.py`** - альтернативное приложение с:
  - Структурированным анализом по главам
  - Группировкой по категориям и методам
  - Интерфейсом с вкладками

### Backend модули:

- **`agent.py`** - основной агент обработки:
  - `refine_quotes()` - улучшение цитат
  - `harvest_all_from_pdf()` - извлечение всех цитат
  - `improve_existing_quotes()` - улучшение существующих
  - `deep_scan_with_gemini()` - глубокое сканирование

- **`parser.py`** - работа с PDF:
  - Извлечение текста
  - Определение темы книги
  - Валидация цитат
  - Перевод (Claude/GPT)

- **`gemini_extractor.py`** - глубокое сканирование:
  - Извлечение всех инсайтов за один запрос
  - Обработка до 500K символов

- **`gemini_book_analyzer.py`** - структурированный анализ:
  - Разбиение на главы
  - Извлечение по категориям
  - Классификация по типам методов

- **`smart_quote_extractor.py`** - умный экстрактор:
  - Анализ контекста
  - Оценка качества
  - Адаптивное извлечение

- **`quote_validator.py`** - валидация:
  - 4 этапа проверки
  - Оптимизация для Threads (≤500 символов)
  - Проверка осмысленности

- **`claude_client.py`** - клиент Claude API (НОВЫЙ):
  - Все функции для работы с Claude
  - Автоматический fallback

---

## 🎯 Основные функции

### 1. Извлечение цитат

**Методы:**

1. **Глубокое сканирование (Gemini)** - кнопка "🚀 ГЛУБОКОЕ СКАНИРОВАНИЕ"
   - Извлекает ВСЕ инсайты за один проход
   - Результат: `data/quotes/{book}_gemini_analysis.json`
   - Время: 30-60 секунд
   - Стоимость: ~$0.015 на книгу

2. **Стандартное извлечение (GPT/Claude)** - кнопка "🔥 Собрать цитаты"
   - Постраничный анализ
   - Результат: `data/quotes/{book}.json`
   - Время: 5-10 минут
   - Стоимость: ~$0.35 на книгу

3. **Улучшение существующих** - кнопка "🧠 Улучшить цитаты"
   - Анализ и улучшение уже извлеченных
   - Добавляет метаданные
   - Использует Claude (или GPT как fallback)

### 2. Валидация цитат

**4 этапа валидации:**

1. Базовая проверка (длина, структура, запрещенные слова)
2. Проверка осмысленности (завершенность мысли, контекст)
3. Оптимизация для Threads (≤500 символов)
4. Финальная проверка качества

### 3. Публикация в Threads

**Два метода:**

1. **Официальный Threads API** (через Instagram Graph API)
   - Более стабильный
   - Требует настройки Facebook App
   - Работает с 2FA

2. **Неофициальный threads-api** (логин/пароль)
   - Быстрая настройка
   - Больше возможностей (лайки, подписки и т.д.)

---

## 🔌 API и интеграции

### Threads API

**Официальный метод:**
- Endpoint: `https://graph.threads.net/v1.0/{IG_USER_ID}/threads`
- Требуется: `THREADS_ACCESS_TOKEN`, `IG_USER_ID`

**Неофициальный метод:**
- Библиотека: `threads_api/src/threads_api.py`
- Требуется: `INSTAGRAM_USERNAME`, `INSTAGRAM_PASSWORD`

### Google Gemini API

- Модель: `gemini-2.5-flash`
- Библиотека: `google-generativeai`
- Требуется: `GEMINI_API_KEY`

### Anthropic Claude API

- Модель: `claude-3-5-sonnet-20241022`
- Библиотека: `anthropic`
- Требуется: `ANTHROPIC_API_KEY`

### OpenAI API (fallback)

- Модель: `gpt-4o-mini`
- Библиотека: `openai`
- Требуется: `OPENAI_API_KEY`

---

## ⚙️ Настройка и установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Основные зависимости:**
- `streamlit` - веб-интерфейс
- `google-generativeai` - Gemini API
- `anthropic` - Claude API
- `openai` - GPT API (fallback)
- `PyMuPDF` - работа с PDF
- `requests`, `aiohttp` - HTTP запросы

### 2. Настройка .env файла

Скопируйте `env_template.txt` в `.env` и заполните:

```env
# Основные AI модели
GEMINI_API_KEY=...
ANTHROPIC_API_KEY=...
OPENAI_API_KEY=... (опционально, fallback)

# Threads API
THREADS_ACCESS_TOKEN=...
IG_USER_ID=...
THREADS_USER_ID=...

# Или неофициальный метод
INSTAGRAM_USERNAME=...
INSTAGRAM_PASSWORD=...
```

### 3. Запуск приложения

```bash
# Основное приложение
streamlit run app.py

# Альтернативное с Gemini
streamlit run app_gemini.py
```

Откройте: **http://localhost:8501**

---

## 📝 Форматы данных

### Стандартный формат (GPT/Claude):

```json
{
  "book": "Название книги",
  "quotes": [
    {
      "page": 12,
      "original": "Оригинальный текст",
      "summary": "Краткая суть",
      "quote": "Текст цитаты",
      "translated": "Перевод на русский",
      "engaging": true,
      "category": "маркетинг",
      "style": "insight",
      "meta": {
        "confidence": 0.9,
        "practical_value": 0.85,
        "length": 234,
        "validation_score": 0.92,
        "threads_ready": true
      }
    }
  ]
}
```

### Формат глубокого сканирования (Gemini):

```json
{
  "book": "Название книги",
  "extraction_method": "gemini_deep_scan",
  "total_pages": 250,
  "total_chars": 125000,
  "total_quotes": 127,
  "quotes": [...]
}
```

---

## 🎓 Рекомендации по использованию

### Для извлечения цитат:

**✅ Используйте Gemini (глубокое сканирование):**
- Дешевле в 23 раза
- Быстрее (30-60 секунд vs 5-10 минут)
- Извлекает больше инсайтов

### Для улучшения цитат:

**✅ Используйте Claude:**
- Лучшее качество редактирования
- Более естественный язык
- Отличная работа с JSON

### Для публикации:

**✅ Используйте официальный Threads API:**
- Более стабильный
- Работает с 2FA
- Официально поддерживается Meta

---

## 📚 Документация

### Основные документы:

1. **README.md** - главная документация проекта
2. **LLM_USAGE_ANALYSIS.md** - детальный анализ использования LLM
3. **CLAUDE_INTEGRATION.md** - документация по интеграции Claude
4. **QUICK_START.md** - быстрый старт
5. **THREADS_API_SETUP_GUIDE.md** - настройка Threads API
6. **README_GEMINI.md** - документация по Gemini
7. **ADMIN_PANEL_GUIDE.md** - руководство по админ-панели

---

## 🔄 История изменений

### Версия 2.0 (2024-11-03)

- ✅ Интеграция Anthropic Claude API
- ✅ Замена GPT на Claude для написания/редактирования
- ✅ Система приоритетов (Claude → GPT → локальная валидация)
- ✅ Обновлена документация

### Версия 1.0

- ✅ Базовый функционал извлечения цитат
- ✅ Интеграция Gemini для глубокого сканирования
- ✅ Интеграция GPT для обработки
- ✅ Публикация в Threads
- ✅ Админ-панель

---

## 🐛 Решение проблем

### "Claude API key not found"

Проверьте `.env` файл - должен быть `ANTHROPIC_API_KEY=...`

### "ModuleNotFoundError: No module named 'anthropic'"

```bash
pip install anthropic>=0.34.0
```

### Цитаты не извлекаются

1. Проверьте что PDF файлы есть в `data/books/`
2. Проверьте API ключи в `.env`
3. Проверьте логи в терминале

### Публикация не работает

1. Проверьте длину цитаты (≤500 символов)
2. Проверьте настройки Threads API
3. Проверьте срок действия токена

---

## ✅ Текущий статус

**Все системы работают:**
- ✅ Google Gemini - интегрирован и используется
- ✅ Anthropic Claude - интегрирован и используется (НОВОЕ)
- ✅ OpenAI GPT - доступен как fallback
- ✅ Threads API - оба метода работают
- ✅ Валидация - многоэтапная система работает
- ✅ Интерфейс - обновлен и работает

---

**Последнее обновление:** 2024-11-03  
**Версия документа:** 2.0

