# Быстрая настройка Threads API (неофициальный метод)

Это руководство поможет вам быстро настроить публикацию в Threads через неофициальную библиотеку threads-api, используя логин и пароль от Instagram.

---

## Зачем это нужно?

Официальный Instagram Graph API от Meta временно не работает для многих пользователей из-за проблем с OAuth авторизацией. Неофициальная threads-api библиотека позволяет публиковать посты через обычный логин/пароль от Instagram.

## Шаг 1: Добавьте учетные данные в .env файл

Откройте файл `.env` и добавьте следующие строки:

```env
# Неофициальная threads-api (логин/пароль от Instagram)
INSTAGRAM_USERNAME=ваш_instagram_username
INSTAGRAM_PASSWORD=ваш_instagram_пароль
```

**Важно:**
- Используйте тот же Instagram аккаунт, который привязан к Threads
- Убедитесь, что вы хотя бы раз заходили в приложение Threads с этого аккаунта
- Файл `.env` должен находиться в корне проекта

## Шаг 2: Проверьте авторизацию

Запустите тестовый скрипт для проверки подключения:

```bash
python test_threads_login.py
```

Этот скрипт:
- Проверит правильность учетных данных
- Авторизуется в Threads API
- Покажет информацию о вашем профиле
- Создаст файл `.token` для быстрого переиспользования сессии

**Что вы должны увидеть:**

```
✅ Успешный вход в Threads API!

📊 Получение информации о профиле...
📱 Ваш User ID: 123456789
   Username: @ваш_username
   Bio: Ваша био
   Подписчиков: 100
   Подписок: 50

🎉 Все работает отлично!
```

## Шаг 3: Запустите приложение

Теперь запустите основное приложение:

```bash
streamlit run app.py
```

В интерфейсе вы увидите надпись:

```
📡 Метод публикации: threads-api (логин/пароль)
```

Это означает, что приложение автоматически выбрало неофициальный метод публикации.

## Шаг 4: Опубликуйте пост

1. Выберите книгу в боковой панели
2. Соберите цитаты с помощью кнопки "Собрать цитаты"
3. Нажмите "🚀 Опубликовать в Threads"
4. Дождитесь сообщения об успешной публикации

---

## Решение проблем

### Ошибка: "Login failed"

**Возможные причины:**
1. Неправильный логин или пароль
2. Аккаунт не подключен к Threads (зайдите в приложение Threads хотя бы один раз)
3. Instagram заблокировал аккаунт
4. Включена двухфакторная аутентификация (2FA)

**Решения:**
- Проверьте правильность `INSTAGRAM_USERNAME` и `INSTAGRAM_PASSWORD` в `.env`
- Убедитесь, что аккаунт активен и не заблокирован
- Если включена 2FA, попробуйте временно отключить
- Удалите файл `.token` и попробуйте снова

### Ошибка: "User not onboarded to threads.net"

**Решение:**
- Зайдите в официальное приложение Threads хотя бы один раз
- Завершите настройку профиля в Threads

### Токен постоянно устаревает

**Решение:**
- Убедитесь, что файл `.token` не удаляется автоматически
- Проверьте, что `.token` добавлен в `.gitignore` (уже сделано)
- Токен сохраняется после первого входа и переиспользуется

### Медленная публикация

**Причина:**
- Первая авторизация может занять 5-10 секунд
- Последующие публикации будут быстрее благодаря кешированию токена

---

## Безопасность

⚠️ **ВАЖНО:**

1. **Никогда не коммитьте `.env` файл в Git**
   - Файл `.env` уже добавлен в `.gitignore`
   - Не публикуйте свои учетные данные в открытом доступе

2. **Используйте надежный пароль**
   - Лучше создать отдельный пароль для приложения через настройки Instagram
   - Регулярно меняйте пароль

3. **Файл `.token` содержит зашифрованные данные**
   - Не передавайте этот файл другим людям
   - Он уже добавлен в `.gitignore`

---

## Дополнительные возможности threads-api

После успешной настройки вы можете использовать threads-api для:

- Публикации постов с изображениями
- Ответов на посты
- Лайков и репостов
- Подписок/отписок
- Получения ленты новостей
- И многое другое

Больше примеров смотрите в файле `threads_api_example.py`.

---

## Что делать, если Meta восстановит OAuth?

Когда официальный Instagram Graph API заработает снова, вы сможете:

1. Настроить официальный метод (см. `THREADS_API_SETUP_GUIDE.md`)
2. Добавить `THREADS_ACCESS_TOKEN` и `IG_USER_ID` в `.env`
3. Приложение автоматически переключится на официальный API

Приложение поддерживает оба метода и автоматически выбирает доступный.

---

## Нужна помощь?

- Проверьте логи в терминале при запуске `streamlit run app.py`
- Запустите тестовый скрипт: `python test_threads_login.py`
- Изучите примеры в `threads_api_example.py`
- Проверьте официальную документацию: https://github.com/Danie1/threads-api

---

## Краткая справка

```bash
# 1. Настройте .env файл
INSTAGRAM_USERNAME=ваш_username
INSTAGRAM_PASSWORD=ваш_пароль

# 2. Протестируйте подключение
python test_threads_login.py

# 3. Запустите приложение
streamlit run app.py

# 4. Готово! Публикуйте посты в Threads
```

✅ **Готово! Теперь вы можете публиковать цитаты в Threads!**
